# Specification

## Summary
**Goal:** Update the site contact email to `shreefireworks@yahoo.com` everywhere it appears in the frontend.

**Planned changes:**
- Update `siteContent.contact.email` to exactly `shreefireworks@yahoo.com`.
- Replace any displayed occurrences of `shreefirebase@yahoo.com` with `shreefireworks@yahoo.com` across the UI (including footer labels).
- Update all `mailto:` links to `mailto:shreefireworks@yahoo.com` (including in the footer and CTA bar).
- Update SEO/meta copy so the contact page description references `shreefireworks@yahoo.com`.

**User-visible outcome:** Users see the correct contact email everywhere on the site and clicking email links opens a new message to `shreefireworks@yahoo.com`.
